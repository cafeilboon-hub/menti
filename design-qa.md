# Design QA — 2번 경로형 서사 개편

검증일: 2026-08-17

## 비교 대상

- 원본 시각 기준: `C:\Users\양성환\.codex\generated_images\01a00d97-76ea-7513-af8e-e3b4c96fe41e\exec-1b6a5d2a-3934-4ef4-8ca5-1b57afbf6a31.png`
- 로컬 구현: <http://127.0.0.1:4173/>
- 데스크톱 구현 캡처: `qa/second-design-desktop-1440x1024-final-02.png`
- 모바일 첫 화면: `qa/second-design-mobile-390x844-final.png`
- 모바일 전후 비교: `qa/second-design-mobile-change-390x844-final.png`
- 전체 나란히 비교: `qa/second-design-comparison-desktop-final.png`
- 집중 비교: `qa/second-design-comparison-hero-final.png`, `qa/second-design-comparison-panel-final.png`
- 화면 단위 첫 장: `qa/page-snap-overview-desktop-final.png`
- 화면 단위 실행 장: `qa/page-snap-execution-desktop-final.png`
- 화면 단위 마지막 장: `qa/page-snap-lessons-desktop-final.png`
- 화면 단위 모바일: `qa/page-snap-overview-mobile-final.png`
- 화면 단위 원본/구현 비교: `qa/page-snap-comparison-desktop-final.png`
- 색상·모션 반영 첫 장: `qa/color-motion-overview-desktop-final.png`
- 표 집중 캡처: `qa/color-motion-problem-desktop-final.png`, `qa/color-motion-connection-desktop-final.png`, `qa/color-motion-execution-desktop-final.png`, `qa/color-motion-results-desktop-final.png`, `qa/color-motion-change-desktop-final.png`, `qa/color-motion-lessons-desktop-final.png`
- 모바일 결과 표: `qa/color-motion-results-mobile-final.png`
- 색상 확장 원본/구현 비교: `qa/color-motion-comparison-overview-final.png`
- 가중 모션 안정 화면: `qa/heavy-motion-overview-desktop-final.png`, `qa/heavy-motion-results-desktop-final.png`
- 가중 모션 중간 프레임: `qa/heavy-motion-results-mid-desktop-final.png`
- 가중 모션 원본/구현 비교: `qa/heavy-motion-comparison-overview-final.png`

## 뷰포트와 정규화

| 구분 | CSS 뷰포트 | 원본 픽셀 | 구현 픽셀 | DPR | 상태 |
|---|---:|---:|---:|---:|---|
| 데스크톱 | 1440 × 1024 | 1487 × 1058 | 1425 × 1013 | 1 | 첫 화면 · 문제 장 표시 |
| 데스크톱 정규화 | 1440 × 1024 | 1425 × 1013 | 1425 × 1013 | 1 | 원본을 구현 캡처 픽셀 크기로 비례 축소 |
| 모바일 | 390 × 844 | 모바일 원본 없음 | 375 × 812 | 1 | 첫 화면과 세로형 전후 비교 |

인앱 브라우저가 스크롤바와 브라우저 표면을 제외한 콘텐츠 영역을 캡처하므로 CSS 뷰포트와 PNG 픽셀 크기가 다릅니다. 원본 목업은 비율을 유지해 구현 PNG와 같은 1425 × 1013으로 축소했고, 브라우저 크롬 없이 같은 첫 화면 상태로 비교했습니다.

화면 단위 전환에서는 데스크톱 `--header: 88px`와 장 높이 `936px`가 1024px 뷰포트를 채우고, 모바일 `--header: 112px`와 장 높이 `732px`가 844px 뷰포트를 채웁니다. 데스크톱에서는 활성화된 모든 장의 `scrollHeight`와 `clientHeight`가 같아 내부 잘림이 없습니다. 모바일은 읽기 가능한 글자 크기와 콘텐츠 전체를 유지하기 위해 한 장의 프레임 안에서 세로 내부 스크롤을 허용합니다.

## 전체 화면 비교 증거

`qa/second-design-comparison-desktop-final.png`에서 원본과 구현을 같은 크기로 한 화면에 배치했습니다.

- 상단 리포트 라벨, 4단계 고정 탐색, 활성 구리색 밑줄, 우측 `다음 내용` 버튼이 같은 위계로 배치됩니다.
- 본문은 왼쪽 핵심 문장, 가운데 3 → 4단계 경로, 오른쪽 결과 6개의 동일한 정보 구조를 유지합니다.
- 하단은 스토리, 상태 안내, 다음 행동의 3열 구조와 아이보리·잉크·구리색 토큰을 유지합니다.
- 원본의 프레젠테이션용 세부 문구는 공개 원고 기준의 더 정확한 표현으로 바꿨고, 협력사명·실명·새 수치는 추가하지 않았습니다.

## 집중 비교 증거

- `qa/second-design-comparison-hero-final.png`: 제목 줄바꿈, 3개 입력 레일, 4개 과정 노드, 6개 결과 레일, Material Symbols 아이콘, 헤더 정렬을 확대 비교했습니다.
- `qa/second-design-comparison-panel-final.png`: Story 제목과 본문, 상태 네 종류, 날짜·기간·요약 메타, CTA 크기와 정렬을 확대 비교했습니다.

중요 텍스트와 노드 아이콘이 전체 축소 이미지에서 작아지므로 위 두 집중 비교를 별도로 확인했습니다.

## 필수 충실도 평가

- **폰트와 타이포그래피** — Pretendard → Noto Sans KR → Malgun Gothic 폴백과 Cascadia Mono 계열 메타 글꼴을 사용했습니다. 첫 비교에서 3줄로 꺾이던 히어로 제목을 원본처럼 2줄로 맞췄고, 제목·본문·상태 라벨 간 위계를 확인했습니다.
- **간격과 레이아웃 리듬** — 1440px에서 헤더, 히어로, 하단 패널의 시작점과 비율이 원본에 가깝습니다. 390px에서는 모든 열을 자연스러운 세로 순서로 재배치했고 문서 가로 넘침이 없습니다.
- **색상과 토큰** — `#faf9f5` 배경, `#141413` 잉크, `#d97757` 구리색, 회색·모래색 상태 토큰을 일관되게 적용했습니다. 보라·파랑 그라디언트나 AI 장식은 없습니다.
- **표 색상 확장** — 원본의 구리색을 주축으로 오커 `#8b641f`, 세이지 `#607b67`, 슬레이트 `#526b78`과 각 옅은 배경 토큰을 추가했습니다. 문제 장의 4개 기반 셀, 연결 장의 3개 접점, 실행 장의 5단계와 3개 제품, 결과 장의 두 축, 전후 비교와 배운 점 카드가 같은 의미 체계로 구분됩니다. 단색 면과 선만 사용해 원본의 편집 디자인 성격을 유지했습니다.
- **이미지와 자산 품질** — 목업에는 사진·제품 이미지·로고 자산이 없습니다. 증빙 사진을 생성하거나 꾸며내지 않았습니다. 과정 아이콘은 임의 SVG나 문자 대신 Material Symbols 아이콘 라이브러리를 사용했습니다.
- **카피와 콘텐츠** — `딸깍 몇 번`, `복제되지 않도록` 같은 과장되거나 구어적인 표현을 각각 제작 흐름 단축과 조기 대응 지원으로 바꿨습니다. 멘토링 결과와 자체 제품 작업을 명시적으로 구분하고 기준일과 공개 제한을 표시했습니다.
- **아이콘과 조작 표식** — 문제·연결·실행·결과 노드는 하나의 Material Symbols 계열로 맞췄고, CTA와 다음 버튼도 같은 화살표 아이콘을 씁니다.
- **반응형과 접근성** — 모바일 상단 장 링크는 각각 93.75 × 52px이며 44px 기준을 넘습니다. 포커스 표시, 본문 건너뛰기, 의미 있는 버튼 라벨, `prefers-reduced-motion`, 색 외의 상태 텍스트를 제공합니다. 중복 ID와 44px 미만 조작 대상이 없고 모바일 문서 가로 넘침도 없습니다.
- **페이지 전환** — 모든 장에 강제 스크롤 스냅을 적용하고, 장 진입 시 직접 자식 요소가 100ms·180ms 간격으로 순차 등장합니다. 퇴장 300ms, 페이드 560ms, 블러·채도 620ms, 확대·이동 820ms로 호흡을 늘렸습니다. 첫 장·배운 점은 부드러운 줌, 문제는 페이드, 연결은 슬라이드, 실행은 줌인, 결과는 줌아웃, 전후 비교는 페이드 상승을 사용합니다. 상단 탭과 다음 버튼에는 iOS식 눌림·스프링 반응을 더했으며, 모션 감소 설정에서는 이동과 전환 시간을 사실상 제거합니다.

## 상호작용 및 브라우저 확인

검증한 동작:

- 첫 화면의 과정 노드와 상단 4개 장 링크
- `다음 내용`으로 문제 장 이동
- `04 결과 → Before → After → What we learned → 처음으로` 순환
- 고정 탐색의 현재 장 표시 갱신
- `motioncore.ai` CTA 두 곳의 외부 링크, 새 탭, `noopener noreferrer`
- 390 × 844 모바일에서 결과 목록과 전후 비교의 세로 흐름
- 1440 × 1024에서 7개 장 모두 `936px` 높이와 장 상단 정렬
- 390 × 844에서 7개 장 모두 `732px` 높이, 문서 가로 넘침 없음
- `#execution`, `#lessons` 직접 접근 시 해당 장 상단과 활성 상태 복원
- 장 전환 후 이전 장의 애니메이션 클래스가 제거되고 현재 장만 `is-entered` 상태 유지
- 페이드 전환: 문제 장 진입 중 투명도 0에서 1로 복귀하고 변형 없이 정착
- 줌인 전환: 실행 장 중간 프레임 `scale(0.9299)`, opacity `0.5042`, blur `1.98px`에서 최종 `scale(1)`, opacity `1`로 정착
- 줌아웃 전환: 결과 장 중간 프레임 `scale(1.0501)`, opacity `0.5508`, blur `1.80px`에서 최종 `scale(1)`, opacity `1`로 정착
- 1440 × 1024의 문제·연결·실행·결과·전후 비교·배운 점 표 색상과 390 × 844 결과 표의 읽기 흐름
- 가중 퇴장: 첫 화면 110ms 프레임에서 opacity `0.0880`, scale `1.0443`, blur `4.56px`, saturation `0.8358`
- 가중 줌인: 실행 장 중간 프레임에서 scale `0.9169`, opacity `0.7492`, blur `1.72px` 후 최종 scale `1`, opacity `1`
- 가중 줌아웃: 결과 장 중간 프레임에서 scale `1.0701`, opacity `0.7012`, blur `2.02px` 후 최종 scale `1`, opacity `1`
- 모바일 390 × 844 실행 장 중간 프레임 scale `0.9108`, opacity `0.7012`; 최종 상단 `112px`, 가로 넘침 없음

브라우저 콘솔 warning/error: 없음.

## Findings

최종 비교에서 남은 실질적인 P0/P1/P2 문제는 없습니다.

## Comparison history

1. **1차 비교 — blocked**
   - [P1] 히어로 제목이 원본의 2줄 대신 3줄로 감겨 중심 위계가 달라졌습니다.
   - [P2] 네 개 과정 노드의 의미 아이콘이 빠져 있었습니다.
   - [P2] 3개 입력과 6개 결과의 분기선이 약해 핵심 `3 → 과정 → 6` 관계가 덜 명확했습니다.
   - [P2] 첫 화면에서 `다음 내용`을 누르면 상세 문제 장을 건너뛰었습니다.
2. **수정**
   - 히어로 열 폭과 수직 위치를 조정해 제목을 2줄로 고정했습니다.
   - Material Symbols 아이콘을 네 과정 노드와 이동 버튼에 추가했습니다.
   - 입력·과정·결과 레일을 연결하는 의미 있는 분기선을 추가하고 모바일에서는 복잡한 선을 제거했습니다.
   - overview 상태를 별도로 관찰해 첫 `다음 내용`이 문제 장으로 이동하도록 수정했습니다.
3. **재비교 — passed**
   - `qa/second-design-comparison-desktop-final.png`과 두 집중 비교에서 위 P1/P2가 해소되었습니다.
   - 390 × 844에서 문서 가로 넘침 없음, 44px 미만 조작 대상 없음, 결과와 전후 비교가 세로로 읽힘을 확인했습니다.
4. **후속 탐색 보강 — passed**
   - 다음 버튼 순서를 `첫 화면 → 01 → 02 → 03 → 04 → Before → After → What we learned → 첫 화면`으로 확장했습니다.
   - 7회 연속 클릭으로 각 해시와 활성 장, 마지막 `처음으로` 복귀를 확인했습니다.
5. **화면 단위 전환 보강 — passed**
   - 7개 장을 각각 `100svh - 고정 헤더` 높이로 고정하고, 마지막 저작권 푸터를 `What we learned` 장 안으로 편입했습니다.
   - 스크롤 스냅과 순차 페이드·이동 전환을 추가하고, 수동 장 이동과 해시 직접 접근에도 같은 활성 상태를 적용했습니다.
   - 데스크톱 1440 × 1024와 모바일 390 × 844에서 장 높이, 가로 넘침, 터치 크기, 7장 순환을 재검증했습니다.
   - `qa/page-snap-comparison-desktop-final.png`에서 원본과 새 첫 장을 같은 픽셀 크기로 다시 비교해 정보 위계와 시각 언어가 유지됨을 확인했습니다.
6. **표 색상·iOS형 모션 보강 — passed**
   - 표와 목록의 의미를 구리·오커·세이지·슬레이트 팔레트로 분리하고, 진한 헤더·옅은 본문·상태 라벨의 3단 위계를 적용했습니다.
   - 장별 `fade`, `slide`, `zoom-in`, `zoom-out`, `fade-rise`, `zoom-soft` 변형을 지정하고 탭 표시와 눌림 반응을 추가했습니다.
   - `qa/color-motion-comparison-overview-final.png`에서 원본 첫 화면과 색상 확장 후 첫 화면을 같은 1425 × 1013 픽셀로 비교했습니다. 히어로·경로·하단 3열 비율은 유지됐고 상태 안내에만 의미 색상이 추가됐습니다.
   - 원본 목업이 정의하지 않은 상세 장의 표 색상과 전환 모션은 사용자 요청에 따른 의도적 확장입니다. 여섯 상세 캡처에서 색상 위계, 본문 대비, 잘림 여부를 별도로 확인했습니다.
   - 데스크톱 7개 장 936px, 모바일 7개 장 732px, 가로 넘침 없음, 52px 장 탭과 60px 다음 버튼을 재확인했습니다.
7. **느린 가중 모션 보강 — passed**
   - 줌인 시작 배율을 `0.90 → 0.86`, 줌아웃 시작 배율을 `1.075 → 1.11`, 슬라이드 이동을 `44px → 60px`로 확대했습니다.
   - 전환 시간과 블러·채도 변화량을 늘리고, 퇴장과 진입이 자연스럽게 겹치도록 진입 시작 간격을 조정했습니다.
   - 반복 관찰 시 애니메이션이 다시 시작되던 가능성을 제거하도록 이미 표시된 장의 `is-entered` 상태를 재사용했습니다.
   - 7회 연속 순환에서 모든 장이 opacity `1`, scale `1`로 안정화되고 `What we learned → 처음` 복귀가 유지됨을 확인했습니다.
   - `qa/heavy-motion-comparison-overview-final.png`에서 정지 상태의 원본 위계가 유지되는지 비교하고, `qa/heavy-motion-results-mid-desktop-final.png`에서 강화된 줌아웃·블러 중간 프레임을 별도로 확인했습니다.

## Follow-up polish

- [P3] 원본은 입력·결과 분기선 모서리가 둥글고 구현은 직각입니다. 정보 관계와 위계에는 영향이 없으며, 향후 회로 경로의 곡률을 브랜드 토큰으로 정하면 더 정교하게 맞출 수 있습니다.
- [P3] Material Symbols는 Google Fonts에서 불러옵니다. 배포 환경에서 외부 폰트 의존성을 없애야 한다면 해당 아이콘 폰트를 저장소에 포함하는 후속 작업이 필요합니다.

## Implementation checklist

- [x] 선택한 2번 목업의 데스크톱 구조 구현
- [x] 13장 발표 흐름을 4장 공개 리포트 구조로 재편
- [x] 내용 정확성·공개 제한·성과 구분 보강
- [x] 데스크톱 원본/구현 나란히 비교
- [x] 히어로·하단 패널 집중 비교
- [x] 모바일 첫 화면·결과·전후 비교 검증
- [x] 04 이후 전후 비교·배운 점·처음 순환과 외부 CTA 검증
- [x] 7개 장의 한 화면 높이·스크롤 스냅·순차 전환 애니메이션 검증
- [x] 화면 단위 원본/구현 나란히 비교와 데스크톱·모바일 캡처
- [x] 6개 상세 장의 표 색상 체계와 모바일 결과 표 검증
- [x] 페이드·슬라이드·줌인·줌아웃·페이드 상승·소프트 줌 중간/완료 상태 검증
- [x] 느린 가중 모션의 퇴장·진입 겹침, 7장 안정화, 모바일 중간 프레임 검증
- [x] 콘솔, 중복 ID, 가로 넘침, 터치 크기 확인

final result: passed
